
# Fix hash size warnings
types_hash_max_size 4096;
types_hash_bucket_size 128;

# Health check endpoint
location /health {
    access_log off;
    return 200 'healthy\n';
    add_header Content-Type text/plain;
}
